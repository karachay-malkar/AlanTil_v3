export function formatClockTime(value){const seconds=Math.max(0,Math.floor(Number(value)||0));return `${Math.floor(seconds/60)}:${String(seconds%60).padStart(2,'0')}`;}
export function formatCompactDate(value,language='ru'){if(!value)return'';const date=new Date(value);if(Number.isNaN(date.getTime()))return'';const locale=language==='tr'?'tr-TR':language==='en'?'en-GB':'ru-RU';return new Intl.DateTimeFormat(locale,{day:'2-digit',month:'2-digit'}).format(date);}
export function formatDuration(seconds,message){const value=Math.max(0,Number(seconds)||0);if(value<60)return message('mobile.common.seconds',{count:Math.round(value)});if(value<3600)return message('mobile.common.minutes',{count:Math.round(value/60)});return message('mobile.common.hours',{count:(value/3600).toFixed(value>=36000?0:1)});}
